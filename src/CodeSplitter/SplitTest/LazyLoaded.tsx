import React from 'react';

export class LazyLoadedComponent extends React.Component {
    render() {
        return <h1>This bad boy was loaded lazily like me on a warm summer day :)</h1>;
    }
}

// export default LazyLoadedComponent;
